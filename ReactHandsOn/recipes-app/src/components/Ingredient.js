import React from "react";
/**
 * hogehoge
 * @param {*} param0 
 * @returns 
 */
export default function Ingredient({ amount, measurement, name }) {
    return (
        <li>{amount} {mesurement} {name}</li>
    );
}
